🔐 ATENÇÃO: Configure sua senha do PostgreSQL!

A linha DATABASE_URL no arquivo .env está usando senha "postgres", mas essa não é a senha correta do seu sistema.

EDITE O ARQUIVO .env E ALTERE ESTA LINHA:

De:
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/lumina"

Para:
DATABASE_URL="postgresql://postgres:SUA_SENHA_AQUI@localhost:5432/lumina"

Substitua "SUA_SENHA_AQUI" pela senha que você digitou quando o comando psql pediu.

Depois execute:
npx prisma migrate dev --name init
